<Window x:Class="SidebarExplorer.App.PresetNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:SidebarExplorer.App.Services"
        SizeToContent="Height"
        Width="{DynamicResource DialogWidth}"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="False"
        ShowInTaskbar="False"
        FontSize="{DynamicResource DialogFontSize}"
        Background="{DynamicResource DialogBackground}">
    <!-- Chrome copied from FilterExtensionsWindow, which copied it from
         AboutWindow, for the reason recorded there: the two button templates
         are eight lines each and a shared dictionary would be another file to
         keep in step. Everything that has to follow Ctrl +/- comes from
         App-level resources. -->
    <Window.Resources>
        <Style x:Key="DialogCloseButtonStyle"
                TargetType="Button">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Width"
                    Value="28"/>
            <Setter Property="Height"
                    Value="28"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="{DynamicResource HoverBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogButtonStyle"
                TargetType="Button">
            <Setter Property="Background"
                    Value="Transparent"/>
            <Setter Property="Foreground"
                    Value="{DynamicResource DialogForeground}"/>
            <Setter Property="BorderBrush"
                    Value="{DynamicResource ControlBorder}"/>
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="FontSize"
                    Value="{DynamicResource DialogFontSize}"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Margin="0,6"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                    Value="True">
                                <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="{DynamicResource HoverBackground}"/>
                                <Setter TargetName="Bd"
                                        Property="BorderBrush"
                                        Value="{DynamicResource ControlHoverBorder}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource PanelBorder}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="{DynamicResource DialogTitleBarHeight}"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                    Background="{DynamicResource DialogHeaderBackground}"
                    MouseLeftButtonDown="HeaderGrid_MouseLeftButtonDown">
                <TextBlock x:Name="TitleText"
                        Foreground="{DynamicResource DialogForeground}"
                        FontSize="{DynamicResource DialogTitleTextSize}"
                        VerticalAlignment="Center"
                        Margin="{DynamicResource DialogTitleIndent}"/>
                <Button Style="{StaticResource DialogCloseButtonStyle}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,6,0"
                        ToolTip="{x:Static services:Strings.ButtonClose}"
                        Click="CancelButton_Click">
                    <Path Data="M4,4 L12,12 M12,4 L4,12"
                            Stroke="{DynamicResource DialogForeground}"
                            StrokeThickness="1.5"
                            StrokeStartLineCap="Round"
                            StrokeEndLineCap="Round"/>
                </Button>
            </Grid>

            <StackPanel Grid.Row="1"
                    Margin="16">
                <TextBox x:Name="NameBox"
                        Background="{DynamicResource ControlBackground}"
                        Foreground="{DynamicResource DialogForeground}"
                        CaretBrush="{DynamicResource DialogForeground}"
                        BorderBrush="{DynamicResource ControlBorder}"
                        BorderThickness="1"
                        FontSize="{DynamicResource DialogFontSize}"
                        MaxLength="24"
                        Padding="6,5"
                        Loaded="NameBox_Loaded"
                        KeyDown="NameBox_KeyDown"/>

                <!-- Says what pressing 확인 will STORE, so it belongs only to
                     the two actions that store anything. A rename keeps the
                     shape already saved, and this line under it read as a
                     promise to replace it (2026-08-11). Collapsed rather than
                     blanked, or the dialog would keep the gap the sentence
                     used to fill. -->
                <TextBlock x:Name="HintText"
                        Foreground="{DynamicResource SecondaryForeground}"
                        FontSize="{DynamicResource DialogHintFontSize}"
                        TextWrapping="Wrap"
                        Margin="0,6,0,0"/>

                <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,18,0,0">
                    <Button Content="{x:Static services:Strings.ButtonCancel}"
                            Style="{StaticResource DialogButtonStyle}"
                            Width="{DynamicResource DialogButtonWidth}"
                            Margin="0,0,6,0"
                            IsCancel="True"
                            Click="CancelButton_Click"/>
                    <Button Content="{x:Static services:Strings.ButtonOk}"
                            Style="{StaticResource DialogButtonStyle}"
                            Width="{DynamicResource DialogButtonWidth}"
                            IsDefault="True"
                            Click="OkButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
