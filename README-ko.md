# Edgetree v2.5.4

[English](README.md)

윈도우 화면 좌우에 상시 고정되는, VS Code 탐색기 방식의 가벼운 파일 탐색 유틸입니다.
기본 윈도우 탐색기를 대체하려는 목적이 아니라, 폴더 구조를 빠르게 훑어보고
파일 위치로 바로 이동하기 위한 보조 도구입니다.

## 다운로드

[릴리즈 페이지](https://github.com/legendsteel11/Edgetree/releases/latest)에서 최신 버전을 받으세요. 매 릴리즈마다 파일 3개가 첨부되어 있습니다.

- **`Edgetree-<버전>-win-x64-setup.exe`** (약 49MB) — 설치 버전. 몇 번 클릭하면 시작 메뉴에 등록되고, 제거도 깔끔합니다.
- **`Edgetree-<버전>-win-x64-standalone.exe`** (약 155MB) — 무설치. 파일 하나를 원하는 곳에 두고 바로 실행합니다.
- **`Edgetree-<버전>-win-x64.exe`** (약 1MB) — 경량. [.NET 8 데스크톱 런타임](https://dotnet.microsoft.com/download/dotnet/8.0)이 설치된 PC에서 쓰는 판입니다.

설정은 `%AppData%\Edgetree`에 있고 셋이 같이 씁니다. 어느 쪽으로 옮겨도 북마크·색상이 그대로 유지되고, 제거해도 남습니다.

## 스크린샷

![화면 왼쪽 가장자리에 도킹된 Edgetree](screenshot/EdgetreeDemo.gif)

## 주요 기능

- **화면 가장자리 고정 배치** — 기본은 좌측, 옵션 메뉴에서 우측으로도 전환
  가능 (작업표시줄을 제외한 작업영역 기준 전체 높이), 고정 상태에서는
  Alt+Tab/작업표시줄에서 제외됨
- **오토하이드**: 헤더의 핀 버튼 한 번으로 화면 가장자리에 얇은 바만 남기고
  완전히 숨겨짐 — 핀이 옆으로 누우면 자동 숨김 중이라는 표시이고, 다시
  누르면 핀이 세워지며 고정으로 돌아옴. 마우스를 가장자리로 가져가면
  자동으로 펼쳐지고, 기본값으로는 마우스가 벗어나면 다시 조용히
  숨겨짐(옵션 메뉴의 "마우스 이탈 시 닫기"를 비활성화하면 대신 바깥을 클릭해야 닫히는
  방식으로 전환 가능). 숨겨졌을 때 남는 얇은 바의 두께도 옵션 메뉴에서
  3~8px로 조절 가능. 가장자리 전체 대신 **화면 가운데의 짧은 손잡이만** 남기는
  것도 선택할 수 있고, 그때의 색도 따로 정할 수 있음
- **드래그로 폭 조절** (고정 상태에서), 우측 리사이즈 라인을 더블클릭하면
  현재 표시된 행 중 가장 긴 내용에 맞춰 폭이 자동으로 늘어나고, 다시
  더블클릭하면 그 전 폭으로 되돌아옴
- **자유 이동 창으로 분리**: 상단 헤더를 드래그하면 고정이 풀리면서
  자유롭게 이동/리사이즈 가능한 일반 창으로 전환(이때는 Alt+Tab/작업표시줄에도
  나타남). 헤더의 핀 버튼을 누르면 다시 가장자리에 고정되고 전체 높이로
  복원 — 고정/자유이동을 오갈 때마다 직전 창 위치와 크기를 기억함
- **드라이브 기반 루트("내 PC")**: 연결된 모든 드라이브를 최상위에 나열,
  폴더는 펼칠 때마다 하위 항목을 지연 로딩
- **이전 상태 그대로 복원**: 펼쳐뒀던 폴더들과 마지막으로 선택했던 위치가
  앱을 다시 실행해도 그대로 돌아옴
- **큰 폴더도 빠르게**: 폴더마다 처음 50개 항목만 표시하고 "… 더 보기 (N개)"
  줄로 나머지를 펼침 — 트리가 한 번에 수천 개 행을 그리지 않아, 파일이 아주
  많은 폴더 아래의 북마크로 이동해도 즉시 반응. 표시 개수는 옵션 메뉴에서
  1~50개로 조절 가능(저해상도 화면 고려)
- **파일 형식 필터**: 하단 바의 칩으로 코드·이미지·문서·미디어·압축·실행
  파일 중 트리에 보일 것을 선택하고 나머지는 트리에서 빠짐. 원하는 확장자를
  직접 지정하거나(사용자 지정), 항상 숨길 확장자를 따로 적을 수 있음(제외).
  검색은 필터로 걸러진 파일도 찾음
- **경로 표시줄**: 경로를 직접 입력해 바로 이동. `Ctrl+←` / `Ctrl+→` 또는
  마우스 앞·뒤 버튼으로 사용했던 폴더를 뒤로·앞으로 오가고, 표시줄에서 그
  목록을 볼 수 있음
- **네트워크 위치**: 드라이브 문자가 없는 공유 폴더(`\\서버\공유`)를 트리 빈 곳
  우클릭 메뉴에서 추가하면 트리에 계속 남음
- **설정 내보내기/가져오기**: 설정을 JSON 파일로 저장하고 다른 PC에서 불러오기
  (북마크 포함, 해당 PC에 없는 경로는 자동으로 정리됨)
- 폴더 행을 한 번 클릭하면 펼침/접힘 (VS Code처럼 화살표뿐 아니라 줄 전체 클릭
  가능), 파일은 더블클릭으로 기본 연결 프로그램 실행
- **멀티 셀렉트**: `Ctrl+클릭`으로 파일/폴더를 하나씩 추가·해제,
  `Shift+클릭`으로 화면에 보이는 순서 기준 범위 선택 — 선택한 항목들을 한
  번에 복사(`Ctrl+C`)·삭제(`Delete`, 확인창 1회)하거나 통째로 앱 밖으로
  드래그. 우클릭 메뉴 상단에 "N개 항목 선택됨"이 표시되고, 단일 대상
  동작(이름 바꾸기·경로 복사)은 자동으로 비활성화됨. `Esc` 또는 일반
  클릭으로 해제
- **폴더 자동 접기** (옵션 "..." 메뉴): 폴더 하나를 펼치면
  그 폴더까지 가는 경로만 남기고 나머지 열려있던 폴더는 자동으로 접힘
- **북마크 패널**: 헤더 아래에 폴더와 파일을 담아두고, 한 번 클릭하면 트리에서 해당
  경로까지 바로 펼쳐지고(대상 폴더 자신도 함께 펼쳐짐) 트리 맨 위에 오도록
  스크롤됨 — 파일이 아주 많은 폴더 아래에 있거나 다른 드라이브에 있어도 한 번의
  클릭으로 안정적으로 이동. 행마다 번호가 붙고 그 번호가 곧 Ctrl+Alt+L·J
  순환 순서이며, 행을 끌어서 순서를 바꿀 수 있음. 담긴 개수에 맞춰 높이가
  자동으로 맞춰지고(구분선 더블클릭으로 정확히 맞춤도 가능), 트리의 글자 크기
  확대/축소도 함께 따라감. 옵션 메뉴에서 탐색기 위/아래 위치 전환 가능(고정/창모드 모두 적용)
- **인라인 이름 바꾸기** (VS Code 방식): `F2` 또는 우클릭 메뉴로 트리 행에서
  바로 이름을 수정, 별도 팝업 없음. 이미 선택된 **파일**을 잠시 뒤 다시 한 번
  클릭해도 이름 바꾸기 시작(탐색기와 동일). 폴더는 클릭이 펼침/접힘이라 제외 —
  `F2`/우클릭 사용
- **북마크** (`Ctrl+Alt+K`): 자주 돌아오는 행에 표식을 달아두고
  `Ctrl+Alt+L` / `Ctrl+Alt+J`로 표시한 곳들을 순서대로 오갑니다. 표식은 행
  오른쪽 끝에 표시되고 앱을 다시 켜도 유지됨
- **폴더 숨기기**: 잘 안 쓰는 폴더나 드라이브를 우클릭 → "숨기기"로
  트리에서 감춰 둡니다. 여러 개를 선택한 뒤 한 번에 감출 수도 있습니다. 감춘 것은 바로 아래 "숨긴 폴더" 목록에 모여 있어
  하나씩 또는 한 번에 되돌릴 수 있고, 검색은 감춘 폴더 안도 그대로 찾습니다
  (검색 결과나 북마크로 그 안에 들어가면 그동안만 트리에 나타납니다)
- **압축 / 압축 풀기**: 우클릭 메뉴에서 선택 항목을 zip으로 묶고(여러 개면
  하나로), `.zip` 파일은 같은 이름의 폴더로 풀기
- **우클릭 메뉴**: 새 폴더, 새로고침, 열기, 연결 프로그램(파일 전용),
  복사/붙여넣기, 압축, 이름 바꾸기, 삭제(휴지통으로, 확인창 포함), 경로 복사,
  터미널에서 위치 열기, 탐색기에서 위치 열기(폴더는 그 폴더가 열리고, 파일은
  상위 폴더에서 선택됨), 속성 — 각각 단축키 지원
  (`F2`, `F5`, `F7`, `Delete`, `Ctrl+C`/`Ctrl+V`, `Enter`) — **이 우클릭 메뉴가
  열려 있는 동안에도 같은 단축키가 그대로 동작**(색상 설정 등 별도 팝업
  창이 열려 있을 때는 해당 없음); 메뉴가 대상 행을 가리지 않도록 항상 그
  아래에 열림
- **이미지 미리보기**: 이미지 파일을 우클릭하면 메뉴 상단에 썸네일과
  형식·픽셀 크기·용량·수정 날짜가 함께 표시됨 — 열지 않고 내용을 확인하고,
  썸네일을 클릭하면 바로 열림
- **파일을 앱 밖으로 드래그** — 탐색기나 다른 앱으로 끌어다 놓기(복사/이동/
  드롭하여 열기 모두 윈도우 표준 방식대로 동작)
- **멀티미디어 패널**: 제목 표시줄의 이미지 아이콘으로 트리 옆에 패널을 엶(좌우
  위치는 옵션에서 반전 가능). **더블클릭으로 열기**를 활성화하면 트리에서
  더블클릭한 파일이 기본 프로그램 대신 이 패널에서 열리고, 파일을 선택하면
  패널이 자동으로 열리는 옵션도 있음. `Backspace`로 패널을 축소하며, 재생
  볼륨은 저장되어 다음 실행에도 유지됨
- **그림 보기**: 휠로 확대·축소, `Ctrl`·`Shift`+휠로 정밀 조정, 확대 상태에서
  드래그로 이동(우측 하단에 내비게이터). 더블클릭으로 맞춤 ↔ 1:1 전환,
  채우기는 패널에 꽉 차게 잘라 보여줌. 일반 형식에 더해 PSD·RAW·JXL도 읽고,
  썸네일 바 크기를 따로 정할 수 있으며, 그림을 다른 앱으로 바로 끌어낼 수 있음.
  마우스 앞·뒤 버튼으로 이전·다음 그림으로 이동
- **슬라이드 쇼**: 이미지 우클릭 또는 `F8`
- **바탕 화면 배경 지정**: 모니터마다 다른 그림을 지정할 수 있음
- **음악은 백그라운드로 이어짐**: 패널을 닫아도 소리는 계속 남. 앨범아트가
  표시되고 `F9`로 패널 위에 시계를 띄울 수 있음
- **영상**: HDR 보정과 자막을 지원. 자막은 크기·위치·싱크를 조정할 수 있고
  (`<` `>`로 싱크 조정), 크기는 영상 크기에 연동되어 해상도와 창 크기에
  관계없이 같은 비율로 표시됨. `F` 키로 창 크기를 영상 비율에 맞게 조정하면
  레터박스가 제거됨(창 모드)
- **시청 중이던 영상**: 다른 폴더로 이동해도 트리 하단에 표시되고, 선택하면
  이전 재생 위치부터 재생됨
- **전체 화면**: 휠클릭 또는 `Enter`, `Esc`로 나옴. 창을 화면 전체로 넓히거나
  지금 창 크기 그대로 채우는 것 중에서 우클릭 메뉴로 선택하며, 고정 상태에서도
  사용 가능. 영상일 때는 썸네일 바가 자동으로 숨겨짐
- **폴더 검색** (`Ctrl+F` 또는 제목 표시줄 검색 아이콘으로 트리 ↔ 검색 화면
  전환, 폴더 우클릭 → **이 폴더에서 검색**도 가능): 폴더를 지정하면 그 폴더를
  인덱싱해 파일명을 즉시 찾음 — 부분일치 또는 `*`/`?` 와일드카드, 결과는
  폴더별로 묶여 표시되고 이름/날짜로 정렬 가능. 결과를 클릭하면 트리에서 해당
  위치로 이동(폴더가 맨 위에 고정)하거나 다른 앱으로 바로 끌어다 쓸 수 있고,
  최근 검색어를 기억함(`↑`/`↓`로 불러오고 개별 삭제 가능). `Ctrl+E`로 트리로
  복귀. **인덱싱 중에도 검색 가능** — 결과가 찾아지는 대로 올라옴
- **검색 인덱스 저장**: 한 번 인덱싱한 폴더는 다음 실행 때 다시 훑지 않고
  즉시 불러옴. 네트워크 드라이브(NAS)처럼 인덱싱이 오래 걸리는 곳에서 특히
  차이가 큼(60만 개 기준 약 6분 → 1초 남짓). 자동으로 다시 훑지는 않으므로
  상태줄에 인덱스가 언제 것인지 표시되고, 갱신은 새로고침 버튼으로
- **정렬**: 이름순/날짜순/유형순/크기순, 오름차순/내림차순 — 옵션("...") 메뉴 또는 폴더
  자신의 우클릭 메뉴에서 바꿀 수 있고, 후자는 그 폴더를 즉시 재정렬해서
  보여줌(다음 번 펼칠 때까지 기다릴 필요 없음)
- **아이콘 종류 2택**: 기본은 **윈도우 탐색기와 동일한 아이콘** —
  exe/바로가기 등은 탐색기처럼 그 파일 고유의 아이콘이 표시되고, 아이콘
  추출은 백그라운드에서 처리되어 네트워크 드라이브에서도 느려지지 않음.
  옵션 메뉴의 "아이콘 종류"에서 Material Icon Theme 기반 아이콘 셋
  ([THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) 참고)으로 전환
  가능(재시작 불필요, 트리·검색 결과·북마크 패널 공통 적용); 드라이브 이름은
  굵게 표시
- 헤더: 고정, **전체 접기**(토글: 눌렀을 때 현재 펼쳐진 폴더들을 기억하고
  전부 접으며, 다시 누르면 기억해둔 상태 그대로 복원), **옵션("...") 메뉴**
  (폴더 자동 접기·폴더 전체 접기·항상 위에 표시·기본 설정(부팅 후 자동
  시작·트레이 아이콘·폴더/파일/드라이브 아이콘·제목 표시줄 텍스트)·북마크
  패널·멀티미디어 패널·표시할 파일 형식·좌우 위치 반전·고정 위치 오른쪽·
  마우스 이탈 시 닫기·숨김 시 손잡이만·숨김 시 막대 두께·색상 설정·아이콘
  종류·정렬 기본값·글꼴 크기·폴더에 표시할 항목 개수·들여쓰기 간격·행 간격·
  스크롤바 두께·언어·설정 내보내기/가져오기·전체 설정 초기화·도움말·앱 정보
  등, 모두 설정 저장됨), 트레이로 최소화, 종료
- **트레이로 최소화**: 트레이 아이콘 클릭(또는 "열기" 메뉴)으로 창 복원,
  우클릭으로 열기/종료
- **색상 설정**: 폴더명/파일명(각각 선택 하이라이트·마우스 hover 색상까지
  별도 지정 가능)·"…더 보기 (N개)" 표시 텍스트·탐색기 배경·북마크 패널 배경·
  북마크 이름·선택된 항목·마우스 hover 배경·탭 구분선/하이라이트·제목 표시줄
  배경·영역 구분선(제목표시줄/북마크 패널/탐색영역 사이 구분선)을 윈도우
  기본 색상 선택창으로 직접 지정, 커스텀 팔레트도 항목을 오가며 유지됨,
  기본값 복원 버튼 포함. 색마다 `#RRGGBB` 입력칸이 있어 웹이나 디자인
  도구에서 복사한 색상 코드를 바로 붙여넣을 수 있음
- **프리셋**: 앱의 모양 전체(창 모드·위치·크기·도킹·색상·파일 형식·현재 폴더)를
  최대 5개까지 저장. `Ctrl+1`~`Ctrl+5`로 전환하고 `Ctrl+Shift+S`로 지금 들어
  있는 프리셋에 덮어씀. 제목 표시줄 우클릭 메뉴와 트리 빈 곳 우클릭 메뉴에도 있음
- **한국어/영어 UI 언어** 전환 (옵션 메뉴에서 선택, 적용하려면 앱 재시작)
- **정보 창**: 버전, 제작자, 빌드 날짜, 라이선스 요약(보증 없이 제공된다는
  안내 포함)
- 트리·북마크 패널 행에 마우스를 올리면 손가락 커서로 바뀜, 헤더 아이콘은 기본
  상태에서 살짝 톤다운되어 있다가 hover 시 밝아짐
- 창 모드(자유 이동)일 때 Windows 11 기본 창처럼 모서리가 둥글게 처리됨
- **디스플레이 배율 대응**: 윈도우 디스플레이 설정의 확대 배율(125%·150%·200% 등)
  에서 화면을 늘려 그리지 않고 해당 배율로 직접 렌더링 — 고해상도 노트북이나
  27인치 이상 4K 모니터에서 글자와 아이콘이 또렷하게 표시됨
- **글꼴 크기**: 옵션 메뉴에서 9~20pt로 조절 가능(기본 12pt), 또는 `Ctrl` `+`/`-`
  단축키. `Ctrl+0`으로 기본값 복원 — 아이콘 크기와 행 간격도 폰트 크기에 비례해서
  같이 조절됨(북마크 패널도 동일하게 적용)
- **들여쓰기 간격**: 폴더 단계마다 들여쓰기 폭(펼침 화살표·가이드라인·아이콘/이름
  정렬 전부 포함)을 옵션 메뉴에서 4~24px로 조절 가능(기본 16px)
- **스크롤바 두께**: 옵션 메뉴에서 6~20px로 조절 가능(기본 12px). 목록 위에
  마우스를 올리면 나타나며, 내용과 겹치지 않도록 자기 영역을 갖습니다
- 선택한 파일의 상위 폴더 들여쓰기 가이드라인이 VS Code처럼 밝게 강조 표시
- 연결 프로그램이 없는 파일은 아무 반응 없이 무시하지 않고, 탐색기와 동일하게
  "어떻게 여시겠어요?" 선택 창으로 대체

## 요구 사항

- Windows 10/11
- [.NET 8 SDK](https://dotnet.microsoft.com/download/dotnet/8.0)
  (소스에서 빌드/실행할 경우)

## 빌드 및 실행

```bash
git clone <this-repo-url>
cd Edgetree
dotnet run --project src/Edgetree
```

솔루션 전체 빌드:

```bash
dotnet build Edgetree.sln
```

## 단일 실행 파일로 배포하기

프레임워크 종속 (용량 작음, 대상 PC에 .NET 8 데스크톱 런타임 필요):

```bash
dotnet publish src/Edgetree -c Release -r win-x64 --self-contained false -p:PublishSingleFile=true
```

독립 실행형 (용량 큼, 대상 PC에 .NET 설치 불필요):

```bash
dotnet publish src/Edgetree -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true
```

`IncludeNativeLibrariesForSelfExtract` 옵션이 핵심입니다 — 이게 없으면 WPF의
네이티브 연동 DLL(`D3DCompiler_47_cor3.dll`, `wpfgfx_cor3.dll` 등)이 exe 안에
포함되지 않고 옆에 따로 풀려서, exe 파일 하나만 옮기면 실행이 안 됩니다.

어느 쪽이든 결과물은 `src/Edgetree/bin/Release/net8.0-windows/win-x64/publish/`
경로에 생성됩니다.

설치 버전은 세 번째 publish로 만듭니다 — 같은 self-contained 빌드에서
`PublishSingleFile`만 빼서 파일을 흩어진 채로 두는 것입니다. 그게 이 판의 존재
이유이기도 합니다. 단일 파일 self-contained는 실행할 때 자기를 메모리로 풀어내서,
.NET이 없는 PC의 작업 관리자에 350~400MB로 잡힙니다. 흩어진 파일은 디스크에서
정상적으로 매핑됩니다.

```bash
dotnet publish src/Edgetree -c Release -r win-x64 --self-contained true -o publish/folder
"$LOCALAPPDATA/Programs/Inno Setup 6/ISCC.exe" installer/Edgetree.iss
```

컴파일은 [Inno Setup 6](https://jrsoftware.org/isinfo.php)이 하고, 결과물은 나머지
둘과 같은 `releases/v<버전>/`에 떨어집니다. 스크립트가 버전을 exe에서 읽어 오므로
csproj만 올리면 됩니다.

## 변경 이력

### v2.5.4 (2026-08-26)

- 파일 형식 필터가 폴더의 파일을 모두 감춘 경우, 감춰진 파일 개수가 폴더 안에 표시됩니다.
- 트리 우클릭 메뉴에서 해당되지 않는 항목이 흐리게 표시되지 않던 문제를 수정했습니다. 파일에서의 `숨기기`, zip이 아닌 파일에서의 `압축 풀기` 등이 해당합니다.
- 다른 앱으로 파일을 끌어놓는 중 그 앱에서 오류가 발생하면 Edgetree도 함께 종료되던 문제를 수정했습니다.
- 트리 우클릭의 `이 폴더 숨기기`가 `숨기기`로 바뀌었습니다. 드라이브와 여러 항목에도 같은 표기를 사용합니다.
- 썸네일 목록의 높이가 스크롤바 폭 때문에 두 값 사이를 오가던 문제를 수정했습니다.

### v2.5.3 (2026-08-24)

- 앱 전체화면을 사용한 뒤 멀티미디어 패널의 파일 정보가 썸네일 바 위에 겹쳐 보이고 패널 높이가 흔들리던 문제를 수정했습니다.

### v2.5.2 (2026-08-24)

- 폴더에 지정한 정렬이 하위 폴더에도 적용됩니다. 하위 폴더에 개별 정렬을 지정한 경우에는 그대로 유지됩니다.
- 제목 표시줄의 `전체 접기` 아이콘을 `Shift`+클릭하면 펼침 상태를 저장하지 않고 접습니다.
- 메뉴가 길어 잘릴 때 상단·하단 화살표에 마우스를 올리면 스크롤됩니다.
- USB, 클라우드 드라이브를 연결하거나 해제하면 트리에 자동으로 반영됩니다.
- 썸네일 목록에서 선택된 항목의 가시성을 높였습니다.
- 음악을 분리 재생 중일 때 제목 우측의 X로 재생을 종료할 수 있습니다.
- `모두 접기` → `전체 접기`, `모든 펼친 폴더 접기` → `폴더 전체 접기`
- `숨김·시스템 항목 표시`가 `옵션 → 기본 설정`으로 이동했습니다.
- 트리에서 `더 보기`에 가려진 항목을 썸네일에서 클릭해도 선택되지 않던 문제를 수정했습니다.

### v2.5.1 (2026-08-22)

- 보고 있는 폴더에 파일이 추가·변경될 때 앱이 종료될 수 있던 문제를 수정했습니다.
- 새로고침 시 내용이 변경된 파일의 썸네일이 갱신됩니다.

### v2.5.0 (2026-08-22)

- 썸네일 바에 세로로 스크롤되는 `썸네일 목록` 배치가 추가되었으며, 기본값으로 적용됩니다.
- 썸네일 크기는 `Ctrl`+휠, 표시되는 줄 수는 경계 드래그로 조정할 수 있습니다.
- 썸네일 목록에서 여러 파일을 선택해 복사, 잘라내기, 삭제, 밖으로 드래그할 수 있습니다.
- 썸네일이 없는 파일은 종류 아이콘으로 표시됩니다.
- 썸네일이 간혹 거꾸로 표시되던 문제를 수정했습니다.
- 썸네일을 클릭해도 이미지가 바뀌지 않던 문제를 수정했습니다.

### v2.4.2 (2026-08-19)

- 시청 중이던 영상이 트리 하단에 표시되며, 선택하면 이전 재생 위치부터 재생됩니다.
- 자막 크기가 영상 크기에 연동됩니다. `<` `>`로 싱크를, 메뉴에서 위치를 조정할 수 있습니다.
- 고정 상태에서도 `바탕화면 채우기`를 사용할 수 있습니다.
- `F` 키로 창 크기를 영상 비율에 맞게 조정할 수 있습니다.
- 마우스 앞·뒤 버튼으로 폴더 이력과 이전·다음 이미지를 이동합니다.
- 재생 볼륨이 저장됩니다.

### v2.4.1 (2026-08-18)

- 프리셋을 연속으로 저장했을 때 트레이 메시지가 계속 나오는 것을 방지했습니다.
- `표시할 파일 종류`가 `표시할 파일 형식`으로 변경되었습니다.
- `바탕화면 전체`가 `바탕화면 채우기`로, `제목 표시줄 타이틀`이 `제목 표시줄 텍스트`로 변경되었습니다.
- 영문 UI의 표기와 문장을 정리했습니다.

### v2.4.0 (2026-08-17)

- 즐겨찾기가 북마크로 통합되었습니다. 기존에 저장한 항목은 그대로 유지됩니다.
- 북마크 패널에서 항목을 드래그해 순서를 변경할 수 있습니다.
- 프리셋에 창 모드가 저장되며, 종료할 때의 모드로 실행됩니다.
- 전체 화면 전환 시 창 크기를 그대로 유지하는 옵션이 추가되었습니다.
- 앨범아트에 맞춤 · 1:1 · 채우기 옵션이 추가되었으며, 선택한 크기는 재시작 후에도 유지됩니다.
- 이미 열려 있는 폴더를 클릭하면 선택만 되고, 접기는 한 번 더 클릭해야 동작합니다.
- 폴더가 임의로 접히거나 트리가 C: 드라이브로 초기화되던 문제를 수정했습니다.

### v2.3.1 (2026-08-17)

- 북마크·즐겨찾기·검색 결과로 이동할 때도 폴더 자동 접기가 적용됨
- 트리 빈 곳 우클릭에서 프리셋 사용
- 앱 전체화면에서 창을 화면 끝까지 넓힐 수 없던 문제 수정

### v2.3.0 (2026-08-16)

- PSD·RAW·JXL 등 패널에서 볼 수 있는 그림 형식 추가
- 패널 위에 시계 표시(F9)
- 프리셋을 Ctrl+1~5로 바꾸고, Ctrl+Shift+S로 덮어씀
- 파일을 선택하면 멀티미디어 패널이 자동으로 열림(옵션)
- 드라이브 아이콘 표시 옵션 추가
- 색상 설정에 펼침 화살표와 하단 칩 색 추가
- 재생중인 음악과 별개로 선택된 음악이 더 쉽게 구분됨
- 더 보기·접기 시 부모폴더 선택, 들여쓰기 안내선 바로 적용
- 펼침 기호 수정 및 들여쓰기 안내선을 중심에 맞춤
- 북마크·즐겨찾기를 추가하면 패널이 그 목록으로 바뀜
- 일부 대용량·네트워크 폴더에서 인덱싱 도중 검색어를 고칠 때 멈추던 문제 수정
- 썸네일 바가 PSD·RAW처럼 큰 파일까지 미리 읽어 느려지던 문제 수정
- 음악 재생 중 앱 전체화면에서 컨트롤 패널이 가리키는 동안 사라지던 문제 수정

### v2.2.0 (2026-08-15)

- 멀티미디어 패널과 트리 위치를 서로 바꿀 수 있음(옵션)
- 일부 북마크 즐겨찾기 이동 시 트리 맨 아래에 붙던 현상 수정
- 멀티미디어 패널을 열고 창 크기를 조절할 때 트리가 저절로 접히던 것
- 자름맞춤 — 그림을 패널에 꽉 채워 보기
- 그림을 볼 때 Ctrl·Shift+휠로 정밀 확대·축소
- 폴더를 우클릭해 그 안의 음악·영상 이어 재생
- 경로 표시줄에 방문한 폴더 목록
- 앱 크기를 더 작게 축소할 수 있음
- 색상 설정에 체인·모노, 목록 끝 그림자 적용

### v2.1.0 (2026-08-14)

- 이미지 슬라이드 쇼(이미지 우클릭 · F8)
- 모니터별로 다른 배경화면 지정(해당 모니터 위에서 지정할 때마다)
- 패널 아래쪽에 반쯤 걸친 항목도 한 번에 클릭
- 여러 개 선택 후 하나를 해제할 때 간혹 발생하던 전체 해제 문제 수정
- 즐겨찾기·북마크로 이동한 뒤 부분적으로 해당 항목이 항상 상단으로 오지 않던 문제 수정

### v2.0.5 (2026-08-13)

- 앱의 탐색기 기능과 윈도우 탐색기 기능을 일치시킴
- 네트워크 위치 추가 기능(빈 곳에 우클릭 메뉴)
- 폴더 내 파일 전체 펼치기 옵션
- 버그 수정 및 안정성 개선
### v2.0.4 (2026-08-13)

- 트리 위치 표시 보완
- 음악 재생 플레이어 기능 정렬

### v2.0.3 (2026-08-13)

- 버그 수정 — 특정 단축키 문제 해결

### v2.0.2 (2026-08-12)

- **이미지 뷰어가 "멀티미디어 패널"이 되었습니다**: 그림만이 아니라 음악과
  영상까지 다루고, 화면을 닫아도 소리는 이어지기 때문입니다
- **더블클릭으로 앱에서 바로 열기**: 옵션 메뉴 → 멀티미디어 패널에서 켜면
  이미지·음악·영상을 더블클릭했을 때 기본 프로그램 대신 앱의 패널에서 엽니다.
  Enter도 같이 따르고, 그 밖의 파일은 지금처럼 기본 프로그램으로 갑니다
- **어느 곡이 울리고 있는지 한눈에**: 앨범아트 위의 표시가 삼각형이면 재생 전,
  막대 둘이면 지금 재생 중인 곡입니다. 커버가 같은 앨범의 곡들도 구분됩니다.
  다른 곡을 보는 동안에는 아래 재생 영역에 바탕이 깔려, 지금 나는 소리가 다른
  파일의 것임을 함께 알려줍니다
- **재생을 눌러도 화면이 움직이지 않습니다**: 곡을 시작할 때 앨범아트 크기가
  달라지던 것을 정리했습니다
- **즐겨찾기 전체 해제**: 즐겨찾기 행 우클릭 메뉴에 추가했습니다. 북마크
  패널에도 같은 자리에 있습니다
- **색상 설정 창 정리**: 테마 줄의 버튼을 여섯에서 넷으로 줄이고, 지금 쓰는
  테마를 파랗게 표시합니다
- **옵션이 바로 저장됩니다**: 옵션 메뉴에서 바꾼 설정이 누르는 즉시 기록됩니다

### v2.0.1 (2026-08-12)

- **트리에 표시되는 위치 관련 로직을 강화했습니다**
- **드라이브 행에 드라이브 종류에 맞는 아이콘이 표시됩니다** — 고정·이동식·
  네트워크·광학 드라이브가 각각의 모양으로 보입니다
- **라이트 테마에서 전체화면 재생 컨트롤이 또렷하게 보입니다** — 재생 바와
  버튼이 어두운 배경 위에서 읽히도록 색을 다시 잡았습니다
- **긴 메뉴가 마우스를 따라 움직이지 않습니다** — 목록이 긴 메뉴에서 아래쪽을
  지나 올라올 때 항목이 밀려 올라가는 일이 있었습니다

### v2.0.0 (2026-08-12)

- **경로 직접 입력** 및 히스토리 기능(`Ctrl+←`, `Ctrl+→`) 추가
- **이미지 뷰어** — 썸네일 바 및 내비게이션
- **영상 재생** — HDR 보정 및 자막 지원
- **음악 재생** — 앱 내에서 전역 플레이어로 설정하고 다른 작업으로 이동 가능
  (예: 이미지 뷰어 또는 파일 관리, 검색 등)
- **프리셋** — 사용자가 정한 앱 형태 및 설정 등을 저장하고 그대로 불러올 수
  있습니다 (5개까지)
- **더 다양해진 랜덤 색상 모드**
- **메모리 관리 및 성능 최적화**, 버그 수정, 앱 속도 향상
- **`F1` 도움말**

### v1.7.1 (2026-08-07)

- 높이를 줄이고 자동 숨김을 함께 쓸 때, 펼쳐지는 순간 드물게 앱이
  종료될 수 있던 문제를 수정했습니다

### v1.7.0 (2026-08-07)

- **화면 높이를 다 쓰지 않아도 됩니다**: 도킹된 앱의 위/아래 가장자리를 끌어
  화면 가장자리에서 차지할 높이를 정할 수 있습니다. 위쪽 가장자리는 위치까지 함께
  옮기므로 한 번의 동작으로 크기와 위치가 정해지고, 가장자리를 더블클릭하면 전체
  높이로 돌아갑니다. 픽셀이 아니라 작업 영역에 대한 비율로 저장되어 다른 모니터에
  연결해도 그대로 재현되며, 자동 숨김 손잡이와 막대도 그 구간을 기준으로 잡힙니다 —
  위쪽 3분의 1만 차지하면 손잡이도 그 안에 둡니다
- **처음 실행하면 손잡이로 시작합니다**: 설정이 없는 새 설치는 화면 옆면을 세로로
  가로지르는 막대 대신, 가장자리 가운데의 짧은 손잡이로 시작합니다. 쓰던 설정이 있으면
  그대로 유지됩니다

### v1.6.0 (2026-08-06)

- **설치 버전 추가**: 인스톨러로도 받을 수 있습니다. 시작 메뉴에 등록되고 제거도
  깔끔하며, 제거해도 설정은 남습니다. 무설치·경량 판은 그대로 있습니다
- **자동 숨김 손잡이**: 숨겼을 때 가장자리 전체가 아니라 화면 가운데의 짧은
  손잡이만 남길 수 있습니다(옵션 메뉴 → "숨김 시 손잡이만"). 가장자리 전체를
  차지하지 않으므로 화면 구석으로 가는 길이나 가장자리를 지나는 드래그를
  가로막지 않습니다
- **손잡이·막대 색상**: 색상 설정 맨 아래에서 숨었을 때의 색을 따로 정합니다.
  이 부분만 바탕화면 위에 놓이므로 배경화면과 부딪히지 않게 맞출 수 있습니다.
  다크/라이트 각각 저장되고, 색상만 내보내기에도 함께 실립니다
- **사용자 지정 확장자**: 표시할 파일 종류 → "사용자 지정…"에 확장자를 쉼표로
  적으면 그 종류가 목록에 생깁니다. 다른 종류와 함께 켤 수 있고, 검색은 지금처럼
  필터와 무관하게 전부 찾습니다
- **창 모드에서 위쪽 리사이즈**: 창 모드일 때 위쪽 가장자리로도 크기를 조절할 수
  있습니다. 최대화된 창은 헤더를 더블클릭하면 원래 크기로 돌아옵니다
- **트레이 아이콘에도 업데이트 표시**: 새 버전이 있으면 트레이 아이콘에 붉은 점이
  붙고 메뉴 맨 위에 한 줄이 생깁니다. 자동 숨김 중이거나 트레이로 내려가 있으면
  옵션 버튼의 점이 보이지 않기 때문입니다
- 영어로 쓰는 환경에서 새 폴더 이름이 한국어로 만들어지던 것과, 드래그하는 동안
  집어 든 행의 선택이 풀리던 것을 고쳤습니다

### v1.5.0 (2026-08-04)

- **색상 피커**: 색상 설정의 색 상자를 누르면 앱 자체 피커가 열리고, 손잡이를
  움직이는 대로 앱에 바로 적용됩니다
- **색상만 내보내기/불러오기**: 색 설정만 파일 하나로 옮깁니다. 다른 PC에서도 같은
  색으로 쓸 수 있습니다
- **북마크 표시를 눌러 해제**: 행 오른쪽 끝의 리본을 클릭하면 바로 풀립니다
- **색 이름표가 자기 색을 입습니다**: 목록에 없거나 상황이 맞아야 보이는 색(더 보기,
  강조, 마우스 오버)도 이름표 자체가 그 색으로 표시되어 고르는 자리에서 바로
  확인됩니다
- 파일 종류나 표시 개수를 바꿔도 보던 자리가 유지되고, 들여쓰기 안내선이 클릭을
  가로채던 것을 고쳤습니다

### v1.4.2 (2026-08-03)

- **표시할 파일 종류**: 하단 바에서 코드 · 이미지 · 문서 · 미디어 · 압축 · 실행 ·
  기타를 눌러 트리에 보일 종류를 고릅니다. 여러 개를 함께 켤 수 있고, "전체"로
  되돌립니다. 옵션 메뉴와 우클릭 메뉴에서도 같은 목록을 씁니다. 폴더는 걸러지지
  않고, 검색은 필터와 무관하게 전부 찾습니다
- **글꼴 굵기**: 보통 / 굵게 / 폴더만 굵게 / 파일만 굵게
- **툴팁이 테마를 따릅니다**. 검색 결과에 커서를 올리면 전체 경로가 보입니다
- **긴 메뉴에 방향 표시**: 위아래로 더 있을 때 화살표가 나타납니다
- **빈 곳 우클릭 메뉴**에서도 북마크 이동 · 숨긴 폴더 · 파일 종류를 쓸 수 있습니다
- **들여쓰기 클릭 수정**: 행 왼쪽 빈 공간을 눌러도 폴더가 접히지 않습니다. 실수로
  드라이브가 통째로 접히면서 화면이 위로 튀던 일이 사라집니다

### v1.4.1 (2026-08-02)

- **패널에 북마크 표시**: 옵션 메뉴 → "패널 표시"에서 왼쪽 패널이 무엇을 보여줄지
  고릅니다 — 북마크 / 즐겨찾기 / 표시 안 함. 북마크 목록에는 위에서부터 번호가
  붙고(`Ctrl+Alt+L`을 몇 번 눌러야 닿는지와 같습니다), 지금 트리가 서 있는
  북마크에 파란 표시가 붙습니다. "표시 안 함"은 항목을 지우지 않고 패널만 접습니다
- **여러 폴더 한 번에 숨기기**: Ctrl·Shift로 여러 폴더를 선택한 뒤 우클릭 →
  "선택한 폴더 숨기기". 안 쓰는 폴더를 한 번에 정리할 수 있습니다
- **긴 메뉴 스크롤**: 항목이 많아 화면보다 길어진 메뉴를 스크롤해서 끝까지 볼 수
  있습니다. 숨긴 폴더 목록이나 북마크 목록이 길어져도 마찬가지입니다
- **폴더 복사 수정**: 폴더를 자기 자신 안에 붙여넣으면 복사가 끝나지 않던 문제를
  고쳤습니다. 이제 그 자리에서 거부합니다
- **북마크 이동 수정**: 북마크로 이동할 때 생기던 문제를 고쳤습니다
- 파일을 지우면 그 파일의 북마크도 함께 정리됩니다
- 새 폴더를 연달아 만들 때 이름 입력칸이 마지막 하나만 열려 있도록 고쳤습니다

### v1.4.0 (2026-08-02)

- **폴더 숨기기**: 우클릭 → "이 폴더 숨기기"로 트리에서 감춥니다. 드라이브도 같은
  방법으로 감출 수 있습니다. 감춘 것은 바로 아래 "숨긴 폴더" 목록에 모이고,
  거기서 하나씩 또는 한 번에 되돌립니다. 검색은 감춘 폴더 안도 그대로 찾고,
  검색 결과나 북마크로 그 안에 들어가면 그동안만 트리에 나타납니다
- **색상 코드 직접 입력**: 색상 설정 창의 색마다 `#RRGGBB` 입력칸이 생겼습니다.
  웹이나 디자인 도구에서 복사한 코드를 붙여넣으면 바로 적용됩니다
- **이동하면 대상이 맨 위로**: 즐겨찾기·북마크·검색 결과로 이동할 때 대상이 화면
  맨 위에 옵니다. 트리 끝이라 더 올라갈 데가 없을 때도 자리를 만들어 올리고,
  파일로 이동할 때는 그 파일 자신이 맨 위입니다. 이동이 한 번에 끝나 중간 위치를
  스치지 않습니다
- **전체 경로 툴팁**: 트리 행에 커서를 올리면 전체 경로가 보입니다. 이름이 길어
  잘린 행도 그 자리에서 확인할 수 있습니다
- **우클릭 메뉴 정리**: 항목을 성격별로 다시 묶었습니다. 새로고침은 파일 명령
  쪽으로, 이 폴더에서 검색은 경로 복사 쪽으로 옮겼습니다
- 그 밖에: 북마크 목록을 우클릭 메뉴에서도 볼 수 있고, 검색 결과가 없을 때
  인덱스 시점을 함께 보여줍니다. 검색 결과 첫 줄 클릭, 자동 숨김 상태에서
  스크롤바를 잡고 창 밖으로 나갈 때, Insert 키가 한글 입력을 방해하던 것을
  수정했습니다

### v1.3.5 (2026-07-30)

- **북마크를 우클릭 메뉴에서**: 우클릭 → 북마크에 지정/해제와 **이전·다음 북마크**가
  함께 들어왔습니다. 단축키도 각 줄에 적혀 있고, 북마크가 없을 때도 목록에서
  단축키를 볼 수 있습니다
- **우클릭 메뉴의 항목 상태**: 상황에 맞지 않는 항목이 흐려지지 않던 것을
  고쳤습니다(파일에서 새 폴더·새로고침, 여러 개 선택했을 때 이름 바꾸기 등)

### v1.3.4 (2026-07-30)

- **잘라내기**: 우클릭 메뉴와 Ctrl+X로 잘라내고 원하는 폴더에 붙여넣으면 복사가
  아니라 이동합니다. 탐색기와 같은 방식이라 양쪽을 오가며 써도 그대로 동작합니다.
  잘라낸 항목은 아이콘이 흐려지고 이름이 기울어져 어느 것을 옮기는 중인지 보입니다
- **검색 목록이 최신인지 알려줍니다**: 검색 중인 폴더 안에서 파일이 생기거나
  없어지면 새로고침 버튼에 파란 점이 붙습니다. 다시 인덱싱하면 사라집니다
- **아이콘 라이선스 고지**: 앱 정보 창에 파일·폴더 아이콘(Material Icon Theme)과
  화면 글리프(구글 Material Symbols)의 출처와 라이선스를 적었고, Apache License
  2.0 전문을 앱이 직접 담고 있어 정보 창에서 바로 열어볼 수 있습니다
- **폴더를 빠르게 접었다 펼칠 때**: 클릭 간격이 짧으면 그 클릭이 반응하지 않는
  경우가 있었는데 수정되었습니다
- 새로고침 버튼이 결과 표시줄 오른쪽으로 옮겨졌고, 검색 기록 목록의 스크롤바가
  앱의 다른 목록과 같은 모양이 되었습니다

### v1.3.3 (2026-07-28)

- **북마크 목록**: 옵션 메뉴에 지금 지정된 북마크가 한눈에 보입니다. 클릭하면
  그 자리로 이동하고(목록은 열린 채로 남아 연달아 확인할 수 있습니다), 행 오른쪽
  `−`로 하나씩, 아래쪽에서 한 번에 해제할 수 있습니다. 단축키도 함께 적어뒀습니다
- **검색 정렬이 메뉴로**: 버튼을 누르면 폴더 그룹 / 이름 / 수정한 날짜와 오름·내림
  차순이 목록으로 열립니다. 다섯 단계를 순서대로 눌러 넘길 필요가 없습니다
- **색상 설정·앱 정보 창이 글꼴 크기를 따라갑니다**: Ctrl +/- 로 키운 만큼 창과
  글씨, 색상 견본이 함께 커집니다
- **작업표시줄 자동 감춤과 함께 쓸 수 있습니다**: 화면 아래 가장자리로 마우스를
  내리면 작업표시줄이 정상적으로 올라옵니다
- 제목 표시줄 아이콘 사이의 빈 곳을 눌러도 창모드로 바뀌지 않습니다
- 재시작한 뒤 즐겨찾기 마지막 줄이 가려지던 것을 고쳤습니다
- 옵션 메뉴의 조절 버튼(+/−)이 글꼴을 키워도 제자리에 있습니다

### v1.3.2 (2026-07-26)

- **자동 숨김 상태에서 드래그로 펼치기**: 다른 창에서 파일을 끌어다 화면
  가장자리의 얇은 바 위에 잠깐 머무르면 앱이 펼쳐집니다. 그대로 원하는
  폴더에 놓으면 됩니다. 빠르게 스쳐 지나갈 때는 열리지 않습니다
- **네트워크 드라이브(NAS 등) 안정성**: 연결된 드라이브가 응답을 멈춰도
  창이 멈추지 않습니다. 응답이 없는 드라이브는 트리에서 사라지지 않고
  자리를 지키며, 회색으로 흐려지고 하위 폴더는 접힌 채로 기다립니다.
  응답이 돌아오면 원래대로 돌아옵니다
- **정렬 기준에 유형·크기 추가**: 이름 / 수정한 날짜 / 유형 / 크기 —
  폴더별로도, 앱 전체 기본값으로도 지정할 수 있습니다. 행의 정렬 아이콘을
  클릭하면 그 폴더의 정렬 메뉴가 바로 열립니다
- **즐겨찾기 순서 바꾸기**: 즐겨찾기 행을 끌어서 원하는 자리에 놓을 수
  있습니다. 마우스를 올리면 전체 경로가 표시되어 같은 이름의 폴더도
  구분됩니다
- **없는 경로가 된 북마크 정리**: 지워진 파일·폴더의 북마크는 자동으로
  정리됩니다. 응답하지 않는 드라이브의 북마크는 그대로 둡니다
- 정보 창에 제작자의 다른 도구(TabStick) 링크 추가

### v1.3.1 (2026-07-25)

- **항상 위에 표시**: 앱을 켠 직후부터 제대로 적용됩니다. 자동 숨김 막대가
  다른 창에 가려 커서에 반응하지 않던 경우도 함께 해결했습니다
- **즐겨찾기**: 이미 그 폴더에 있을 때 즐겨찾기를 다시 클릭하면 목록을 한참
  내려둔 상태여도 그 폴더가 다시 맨 위로 올라옵니다
- **폴더에 표시할 항목 개수**: 스테퍼를 연달아 눌러도 즉시 반응합니다

### v1.3.0 (2026-07-25)

- **북마크**: 파일이나 폴더에 표식을 달아두고(`Ctrl+Alt+K`) 표시한 곳들을
  오갑니다(`Ctrl+Alt+L` / `Ctrl+Alt+J`) — 표식은 앱을 다시 켜도 유지됩니다
- **압축 / 압축 풀기**: 우클릭 메뉴에서 선택한 항목을 zip으로 묶고(여러 개면
  하나로), zip 파일은 같은 이름의 폴더로 풉니다
- **새 폴더 단축키**: `F7`
- **빠른 스크롤**: 트리에서 `Ctrl`을 누른 채 스크롤
- **네트워크 드라이브 표시**: 네트워크 드라이브에 있는 폴더는 아이콘에 작은
  배지가 붙습니다
- **펼침 상태 유지**: 붙여넣기·삭제·이름 바꾸기·드래그로 넣기 뒤에도 펼쳐두었던
  폴더와 보고 있던 위치가 그대로 유지됩니다
- **파일 위에 드롭**: 파일 행에 놓아도 그 파일이 있는 폴더로 들어갑니다
- 저해상도 화면을 위한 우클릭 메뉴 간격 정돈, 즐겨찾기 추가 시 패널 크기 유지

### v1.2.2 (2026-07-23)

- **네트워크 드라이브 개선**: 절전 중이거나 잠시 응답이 없는 드라이브도
  트리에서 자리를 유지하고, 응답이 돌아오면 정상적으로 펼쳐집니다
- **새 버전 다운로드 링크**: 새 릴리즈가 있으면 정보 창의 버전 아래에
  바로 받을 수 있는 링크가 표시됩니다
- 업데이트 알림 점 위치 다듬기

### v1.2.1 (2026-07-23)

- **우클릭 메뉴 이미지 미리보기**: 이미지 파일을 우클릭하면 썸네일과
  형식·픽셀 크기·용량·수정 날짜가 메뉴 상단에 표시 — 썸네일 클릭으로 바로 열기
- **핀 버튼이 고정/자동 숨김 토글로**: 핀을 누르면 자동 숨김(핀이 누움),
  다시 누르면 고정(핀이 세워짐) — 기존의 앱 아이콘 클릭 방식은 핀으로 통합
- **메뉴가 글자 크기를 따라감**: 우클릭/옵션 메뉴의 글자·간격이 트리의
  Ctrl `+`/`-` 확대·축소와 연동, 행간과 그룹 간격도 정돈
- **윈도우 탐색기 아이콘이 기본값으로** (Material 아이콘 셋은 옵션에서 선택)
- **새 버전 알림**: 새 릴리즈가 있으면 옵션(...) 버튼에 작은 점으로 표시
- **다른 앱 사용 중의 동작 정리**: 선택 하이라이트가 옅어져 현재 작업 중이
  아님을 표시하고, 이름 편집이 앱 전환을 넘어 이어지지 않음
- **폴더 내용 반영 개선**: 접어 뒀던 폴더를 다시 펼치면 그 사이의 변경이
  바로 반영되고, 외부에서 파일이 추가·삭제될 때의 화면 갱신이 더 안정적으로
- 다중 선택(Shift+클릭 범위 등)·우클릭 메뉴 조작 다듬기, 색상 설정이 변경
  즉시 저장되도록 개선

### v1.2.0 (2026-07-21)

- **다중 선택**: Ctrl/Shift+클릭으로 여러 파일·폴더를 선택해 한 번에
  복사·삭제하거나 앱 밖으로 드래그
- **아이콘 방식 선택**: 윈도우 탐색기와 동일한 아이콘 옵션 추가

이전 버전의 변경 내용은 [GitHub 릴리즈](https://github.com/legendsteel11/Edgetree/releases)에서 확인할 수 있습니다.

## 요청·버그 신고

pjh85336@gmail.com 으로 보내주세요.

## 같은 개발자의 다른 도구

[TabStick](https://tabstick.com/) — 창에 붙는 인덱스 메모.

## 라이선스

MIT — [LICENSE.md](LICENSE.md) 참고. 파일·폴더 아이콘은 Material Icon Theme
프로젝트(MIT), 화면 글리프는 구글 Material Symbols(Apache License 2.0)에서
가져왔습니다 — [THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md) 참고. Apache
라이선스 전문은 앱이 직접 담고 있고(정보 창에서 열 수 있습니다), 저장소에서는
[src/Edgetree/Resources/APACHE-2.0.txt](src/Edgetree/Resources/APACHE-2.0.txt)에
있습니다.

## 개발 과정에 대하여

이 도구는 제작자가 직접 기획하고 다듬었으며, 구현은 Claude Code(Anthropic)와
협업하여 진행했습니다. 기능 결정, UX 설계, 실사용 테스트 모두 매일 직접
사용하면서 나온 결과물입니다.
