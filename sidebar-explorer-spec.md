# 좌측 고정형 파일 탐색기 유틸 - 기술 시방서

> **구현 현황 (2026-07-16 기준): 1차 목표(MVP) + 2차 확장(우클릭 메뉴/즐겨찾기) 대부분 구현 완료.**
> 최신 사용자 대상 기능 목록/설치 방법은 [README-ko.md](README-ko.md) 참고.
> 이 문서는 최초 기획 시방서이며, 실제 구현 과정에서 아래와 같이 범위가 조정/추가됨:
> - 폴더는 화살표뿐 아니라 **단일 클릭**으로 확장/축소 (당초 "더블클릭"에서 VS Code 방식으로 변경)
> - **자유 이동 창 분리 + 핀 고정** 기능 추가 (헤더 드래그 → 고정 해제, 핀 버튼 → 재고정) — 당초 "이동 방향 없음" 방침에서 확장
> - **모두 접기 버튼**, **헤더 종료(X) 버튼**, **Ctrl +/- 폰트 확대축소** 추가
> - 연결 프로그램이 없는 파일은 탐색기와 동일하게 "어떻게 여시겠어요?" 선택 창으로 폴백
> - **즐겨찾기 패널 + 우클릭 메뉴 확장(열기/연결프로그램/복사·붙여넣기/이름변경/삭제/경로복사/터미널/속성) + 단축키** 추가 구현 완료
> - **이름 바꾸기는 VS Code 방식 인라인 편집**으로 전환 (별도 팝업 창 제거)
> - **트레이 최소화 + 옵션("...") 메뉴**(Auto Collapse, 항상 위에 표시, 윈도우 시작 시 실행, 트레이 아이콘 항상 표시) 추가
> - **헤더 더블클릭으로 고정/자유이동 전환**, 즐겨찾기 패널 UX 다듬기(자동 높이 맞춤, 폰트 확대축소 연동, 접힘 시 함께 숨김) 추가
> - **새 폴더/새로고침(F5)/정렬(이름·날짜, 오름·내림차순)** 우클릭 메뉴 추가, **파일 드래그 아웃**, **색상 설정** 창, **한국어/영어 언어 전환**, **정보(About) 창** 추가
> - Windows 11 둥근 모서리(창모드), 스크롤바/리사이즈 그립 여백, 마우스 호버 시 상위 폴더 배경 번짐 등 시각적 버그 다수 수정
> - **오토하이드**(접힘 상태에서 한 번 더 클릭 시 화면 가장자리 슬리버로 숨김, 마우스오버로 펼침/핀 고정) + **우측 고정 옵션**(옵션 메뉴에서 선택, 기본 정렬/간격 유지) 추가 — 당초 "비범위"였던 다중 방향 고정을 우측까지 확장(가상 데스크톱 전역 표시는 계속 비범위)
> - **앱 종료/재시작 시 트리 상태 복원**(펼쳐진 폴더 목록 + 마지막 선택 위치), **고정 상태 ↔ 창모드 전환 시 직전 창 위치/크기 기억**(같은 실행 세션 내) 추가
> - **파일 아이콘 표시 토글**(폴더 아이콘 토글과 독립), **멀티모니터 포커스 고정**(창모드에서 다른 모니터로 옮긴 뒤 고정 시 그 모니터 기준으로 고정 — 당초 "비범위"에서 구현으로 전환) 추가
> - **색상 설정 확장**: 폴더명/파일명 텍스트 색상을 각각 기본·선택 하이라이트·마우스 hover로 분리(총 6개, 기존 일반/하이라이트/hover 텍스트 3개를 대체), **영역 구분선**(제목표시줄/즐겨찾기/탐색영역 사이) 색상 신설, 색상 설정 창 순서 재정리 — 색상 항목 총 14개로 확장. 메뉴 명칭 "색상 변경" → "색상 설정"으로 통일, "도킹"/"고정" 혼용 표현을 "고정"으로 통일(핀 버튼 툴팁도 좌/우 고정 방향에 따라 동적으로 표시)

## 1. 개요
윈도우 바탕화면 좌측에 상시 고정되는, VS Code 탐색기(Explorer) 방식의 트리형 파일 탐색 유틸.
기본 윈도우 탐색기를 대체 목적이 아니라, **빠르게 파일 위치를 확인/열기 위한 보조 도구**로 사용.

## 2. 창 배치 및 동작 규칙

| 항목 | 사양 |
|---|---|
| 위치 | 화면 좌측 고정 (x=0, y=0) 이 기본. **(구현됨) 옵션 메뉴에서 우측 고정으로 전환 가능** (내용 정렬/간격은 그대로, 리사이즈 시 좌측으로 확장) |
| 높이 | 화면 높이 - 작업표시줄 높이 (작업표시줄 위치 무관하게 작업영역 기준) |
| 폭 (펼침) | 약 260~300px, **우측 경계 드래그로 리사이즈 가능 (1차 목표 포함)**, 마지막 폭 기억, 최대 1200px까지 확장 가능 |
| 폭 (접힘) | 약 40~48px (아이콘 바만 표시) |
| 폭 (오토하이드) | **(구현됨)** 접힘 상태에서 아이콘을 한 번 더 클릭하면 3px 슬리버로 축소, 마우스오버 시 전체 폭으로 펼쳐졌다가 포커스 이탈 후 약 0.4초 뒤 재축소. 펼쳐진 상태의 핀 아이콘으로 오토하이드 해제 |
| 상태 전환 | 상단 아이콘 클릭 → 폭 애니메이션 토글 (펼침 ↔ 접힘 ↔ 오토하이드) |
| 이동 방향 | 좌측 고정이 기본, **(구현됨) 우측 고정 옵션 추가**. 헤더를 드래그하면 고정 해제 → 자유 이동/리사이즈 가능한 일반 창으로 전환, 헤더의 핀 버튼으로 재고정 |
| 작업표시줄 처리 | 고정 상태: Alt+Tab/작업표시줄 제외. 자유 이동 상태: 일반 창처럼 노출 (재고정 시 다시 제외) |
| 항상 위 여부 | 기본은 비활성(옵션에서 켤 수 있음). **오토하이드 작동 중에는 사용자 설정과 무관하게 항상 위로 강제**(그렇지 않으면 최대화된 창에 가려져 슬리버에 마우스가 닿지 않음) — 오토하이드 해제 시 원래 설정으로 복원 |
| 시작 동작 | 프로그램 실행 시 즉시 좌측(또는 설정된 방향) 고정 상태로 배치, 트레이 상주. **재시작 시 항상 고정 상태로 시작** (자유 이동 위치/크기는 같은 실행 세션 내에서만 기억). **(구현됨) 펼쳐진 폴더 목록과 마지막 선택 위치는 재시작 후에도 복원** |

## 3. 화면/UI 구성 (VS Code 탐색기 기준)

- 상단: 접기/펼치기 토글 아이콘 + (펼침 시) 현재 루트 경로 표시
- 트리 영역
  - 폴더 펼침/접힘 화살표, 들여쓰기 가이드라인
  - 폴더+파일이 하나의 트리에 표시되는 VS Code 방식 (좌측 트리/우측 리스트로 분리된 윈도우 탐색기 방식 지양)
  - 파일/폴더 아이콘: **VS Code Material Icon Theme(MIT License, PKief/vscode-material-icon-theme) 사용**
    - 해당 프로젝트의 `material-icons.json` 매핑(확장자/파일명/폴더명 → 아이콘, 폴더 열림/닫힘 아이콘 포함)을 그대로 파싱해 재사용
    - 아이콘 원본은 SVG → 빌드 타임에 트리 표시 크기(16~20px 기준 1x/2x)로 사전 래스터화(PNG)하여 임베드, 런타임 SVG 렌더링 의존성 없이 처리
    - 라이선스 고지(원저작자 표기)를 앱 내 어바웃 화면 또는 README에 유지
    - 2차 고도화 시 `SHGetFileInfo` 연동해 실제 셸 아이콘과 동일화하는 옵션도 고려 가능
  - 선택/포커스 하이라이트 (VS Code와 동일한 톤)
- 검색/필터: **범위 제외** (해당 유틸의 목적상 불필요로 판단)

## 4. 기능 범위 (MVP → 확장 순)

**MVP (1차 목표) — 전체 구현 완료**
- 좌측 고정 창 배치 + 접기/펼치기 애니메이션
- **폭 드래그 리사이즈** (우측 경계 드래그, 마지막 폭 기억)
- 트리 루트: **"내 PC" 방식 — `DriveInfo.GetDrives()`로 연결된 전체 드라이브를 최상위 노드로 나열**
- 폴더 확장/축소 (지연 로딩: 하위 항목은 펼칠 때 조회)
- 파일 더블클릭 → 기본 연결 프로그램으로 열기 (`ShellExecuteEx` + `SEE_MASK_INVOKEIDLIST`, 탐색기와 동일한 셸 협상 경로 사용)
- **폴더는 단일 클릭으로 트리 내 확장/축소** (화살표 클릭도 별도 지원) — 당초 "더블클릭"안에서 VS Code 방식으로 변경
- 우클릭 컨텍스트 메뉴: "탐색기에서 위치 열기" 정도만 최소 지원
- 파일/폴더 아이콘: VS Code Material Icon Theme 기반 (위 3번 항목 참고)
- 접근 불가 폴더(`UnauthorizedAccessException`) 발생 시 해당 노드만 스킵/비활성 처리, 앱 크래시 방지
- 연결 프로그램이 없는 파일: 조용히 무시하지 않고 탐색기와 동일한 "어떻게 여시겠어요?" 선택 창(`OpenAs_RunDLL`)으로 폴백

**MVP 범위 밖에서 추가 구현된 기능**
- **자유 이동 창 분리 + 핀 고정**: 헤더 드래그 → 고정 해제(일반 창으로 전환, `WindowChrome` 기반 네이티브 이동/리사이즈) → 핀 버튼으로 재고정
- **모두 접기** 버튼, 헤더 **종료(X)** 버튼
- `Ctrl` `+`/`-`로 트리 폰트 크기 확대/축소(9~16pt), `Ctrl+0` 기본값 복원 (행 간격도 비례 조절)
- 긴 파일명 말줄임표(`...`) 처리, 선택 시 흰색 텍스트, 들여쓰기 가이드라인(선택 파일의 직속 상위 폴더만 강조), 회전 화살표 등 VS Code 톤 시각 다듬기
- 배포용 단일 실행파일(`dotnet publish` + `PublishSingleFile`), MIT 라이선스/README 작성, 커스텀 앱 아이콘
- **즐겨찾기 패널**: 헤더 아래 `GridSplitter`로 높이 조절 가능한 영역, 폴더 우클릭 → 즐겨찾기 추가, **한 번 클릭** 시 "내 PC" 트리에서 해당 경로까지 자동 펼침 + 대상이 트리 맨 위에 오도록 스크롤 (트리 루트는 계속 "내 PC" 고정 — 별도 루트 전환 아님). 파일이 매우 많은 폴더 아래·다른 드라이브에 있어도 한 번의 클릭으로 안정적으로 이동
- **폴더 표시 개수 제한(가상화 부담 완화)**: 각 폴더는 정렬 후 상위 N개(`FileSystemItem.DisplayCap`, 기본 25, 옵션에서 1~50 조절 가능)만 표시하고 나머지는 "… 더 보기 (N개)" 행 뒤에 보관, 클릭 시 전체 표시. 다른 즐겨찾기로 이동할 때 펼쳐 놨던 폴더는 다시 기본 개수로 복귀. WPF `TreeView` 가상화가 한 번에 수천 개 컨테이너를 실현하지 않게 하는 장치
- **즐겨찾기 이동 신뢰성의 핵심 3요소** (오랜 버그의 원인이 서로 독립적인 3개였음 — 하나만 고쳐선 해결되지 않았음):
  1. 위의 표시 개수 제한
  2. `_navigationToken`은 `NavigateToPath` 시작 시에만 증가. **`SelectedItemChanged`에서는 절대 증가시키지 말 것** — walk 자체가 선택 변경을 유발(자동 접기로 이전 드라이브가 접히면 WPF가 그 드라이브 루트를 선택)하므로, 진행 중인 walk가 스스로를 무효화시켜 두 번 클릭해야 했음
  3. `ExplorerTree`에 `VirtualizingPanel.CacheLength="1000,1000"` + `CacheLengthUnit="Item"` (+ `ScrollUnit="Pixel"`). **다른 드라이브 즐겨찾기 먹통의 진짜 원인.** C:가 2165px로 펼쳐진 상태에서 뷰포트가 432px면, 2165px 아래의 `E:` 행이 기본 캐시(뷰포트 앞뒤 1페이지)를 벗어나 컨테이너가 영원히 생성되지 않음 → walk가 `step[0]`(드라이브 루트 자신)에서 실패. 창모드에서만(뷰포트가 작아서), 표시개수가 클수록(C:가 길어져서) 재현된 이유. 드라이브 루트는 몇 개뿐이라 전부 캐싱해도 비용 없음. 이 캐시는 `ExplorerTreeViewItemStyle`에서 로컬값으로 막아 하위 폴더에는 상속되지 않게 함
  - **시도했다가 실패한 방법(재시도 금지)**: 재시도 횟수 증가(10→40 — 캐시 밖 컨테이너는 다시 본다고 생기지 않음), 폴더별 `IsVirtualizing=false`(하위 폴더는 원인이 아니었음), `ExplorerTree`에 `IsVirtualizing=false`(이 속성은 **상속**되어 트리 전체의 가상화가 꺼지고 스크롤이 앱 전역에서 깨짐), 강제 중앙정렬 스크롤, walk 전 자동 접기
- **우클릭 컨텍스트 메뉴 확장**: 열기, 연결 프로그램으로 열기, 복사/붙여넣기(이름 충돌 시 자동 번호), 이름 바꾸기(입력창), 삭제(휴지통, 확인창 포함), 경로 복사, 터미널에서 열기, 속성 — 다크 테마 + 단축키 힌트(`InputGestureText`) 적용
- **키보드 단축키**: `F2` 이름 바꾸기, `Delete` 삭제, `Ctrl+C`/`Ctrl+V` 복사·붙여넣기, `Enter` 열기, `F5` 새로고침 (컨텍스트 메뉴와 동일 로직 재사용). **컨텍스트 메뉴가 열린 상태에서도 동작** — 열린 메뉴가 키보드 포커스를 가져가 트리의 `KeyDown`이 키를 못 받으므로 메뉴에 `PreviewKeyDown`으로 동일 단축키를 미러링. 단 `Enter`는 메뉴 항목이 하이라이트되지 않았을 때만 "열기"로 처리(하이라이트 상태에선 해당 항목 실행이 정상 동작). `F2`는 메뉴를 먼저 닫고 포커스 복원이 끝난 뒤 실행 — 그렇지 않으면 메뉴가 닫히며 포커스를 되가져가 인라인 rename이 즉시 커밋됨
- **느린 더블클릭 이름 바꾸기**: 이미 선택된 **파일**을 다시 한 번 클릭하면(더블클릭 판정 시간 + 여유만큼 대기 후) 인라인 rename 시작. 대기 시간은 `SystemInformation.DoubleClickTime`을 읽어 사용. 빠른 더블클릭(열기)·드래그 시작·다른 항목 클릭 시 예약 취소. **폴더는 제외** — 이 앱은 폴더 클릭이 펼침/접힘 토글이라, 선택된 폴더를 접는 정상 동작이 매번 rename으로 바뀌어 버림
- **오토하이드 + 우측 고정**: 제목표시줄의 "<" 화살표 버튼을 없애고 앱 아이콘 자체가 접기/펼치기 토글 역할(양방향)을 겸하도록 변경, 접힘 상태에서 아이콘을 한 번 더 클릭하면 3px 슬리버로 숨김 → `Window.MouseEnter`/`MouseLeave`(약 0.4초 지연)로 펼침/재숨김, 펼쳐진 상태의 핀 아이콘으로 해제. 전역 마우스 후크나 커서 폴링 없이 WPF 자체 마우스 이벤트만으로 구현(안전/저위험 우선). 우측 고정은 옵션 메뉴 토글(`AppSettings.DockOnRight`) 하나로, 창 위치 계산(`PositionToWorkArea`)과 리사이즈 손잡이 방향·드래그 부호만 조건 분기 — 내용 정렬/간격은 그대로 유지
- **탐색기 상태 영속화**: 종료 시 펼쳐진 폴더 전체 경로(드라이브 루트 포함)와 마지막 선택 경로를 저장, 다음 실행 시 얕은 경로부터 순서대로 재현. 존재하지 않게 된 경로(드라이브 분리·폴더 삭제/이동)는 조용히 건너뜀
- **고정 상태 ↔ 창모드 위치 기억**: 고정 직전의 자유 이동 상태 Left/Top/Width/Height를 메모리에 저장해뒀다가, 다음 창모드 전환 시 그대로 복원(모니터 구성이 바뀐 경우를 대비해 현재 가상 화면 범위로 클램프). 같은 실행 세션 내에서만 유지, 앱 재시작 시에는 초기화
- 앱 아이콘을 새 `icon.png` 기반으로 교체(실행파일/창/트레이/정보창 아이콘 전부 갱신), 색상 기본값 일부 조정, 헤더 버튼(최소화/옵션/닫기) 정렬 미세 수정, 즐겨찾기 fit 하단 여백 축소 및 폰트 크기 변경 시 자동 재적용
- **파일 아이콘 표시 토글**: 기존 "폴더 아이콘 표시" 옵션과 완전히 독립된 별도 토글(`AppSettings.ShowFileIcons`) - 트리 행의 `IsDirectory` 바인딩에 따라 폴더/파일 아이콘을 각각 다른 `Visibility` 리소스로 제어
- **멀티모니터 포커스 고정**: 창모드에서 다른 모니터로 옮긴 뒤 고정 시, 항상 주 모니터가 아니라 **창이 실제로 있던 모니터** 기준으로 고정되도록 수정. `System.Windows.Forms.Screen.FromHandle`로 현재 모니터를 찾고, `VisualTreeHelper.GetDpi`로 얻은 이 창의 현재 DPI 배율로 그 모니터의 물리 픽셀 작업영역을 WPF 단위로 환산(`GetCurrentMonitorWorkArea`) - 같은 모니터 안에서만 좌표를 변환하므로 모니터 간 DPI 불일치를 신경 쓸 필요가 없음. 보조 모니터에서 오토하이드가 살짝 움직이자마자 바로 닫혀버리던 문제도 함께 리포트됨 — 마우스가 실제로는 창 근처에 있는데도 라우팅된 `MouseLeave`가 잘못 발생하는 것으로 보여, 닫기 직전 OS의 실제 커서 좌표(`Cursor.Position`)를 한 번 더 확인해 근처면 닫지 않고 계속 폴링하도록 방어 로직 추가(근본 원인은 모니터 경계의 OS 레벨 커서 이벤트 정밀도 이슈로 추정, 완전한 재현 환경 없이 방어적으로만 대응)
- **색상 설정 확장**: 기존 "일반 폰트"/"하이라이트 폰트"/"hover 텍스트" 3개를 폴더명/파일명 각각의 기본·선택 하이라이트·마우스 hover 색상(총 6개)으로 분리 — `ShowFileIcons`로 아이콘을 전부 숨겨도 텍스트 색으로 폴더/파일을 구분할 수 있게 하기 위함. 파일 행은 트리의 `HierarchicalDataTemplate`에 자체 `Foreground` 트리거를 둬 상속을 끊고, 폴더 행은 기존처럼 `TreeViewItem` 레벨 트리거의 상속에 의존(변경 없음). 파일 행의 hover 감지는 `RowBorder`가 다른 템플릿(`TreeViewItem`의 `ControlTemplate`)에 있어 `ElementName`으로 못 넘어오므로, `RelativeSource AncestorType=Border`로 시각 트리를 타입 기준 탐색. 제목표시줄/즐겨찾기/탐색영역 사이 구분선도 기존에 재사용하던 "탭 구분선" 색상에서 분리해 **영역 구분선**이라는 전용 색상 신설(총 14개 색상 항목). 색상 설정 창 항목 순서 재배치, 옵션 메뉴 "색상 변경" 명칭을 "색상 설정"으로 통일
- **"도킹"/"고정" 용어 통일**: 우측 고정 옵션 추가 과정에서 일부 신규 문구·주석에 "도킹"이 섞여 들어갔던 것을 전부 "고정"으로 재통일(핀 버튼 툴팁도 "좌측에 고정"으로 고정되어 있던 것을 우측 고정 옵션에 따라 동적으로 표시하도록 수정), README/본 문서의 동일 혼용 표현도 함께 정정

**2차 확장 (미구현, 향후 검토)**
- 드래그 앤 드롭으로 파일 이동
- 셸 아이콘 완전 동일화 (`SHGetFileInfo`) — Material Icon Theme로 충분해 보류
- 검색/필터 (필요성 재검토 후 결정)
- 여러 즐겨찾기 루트 간 탭 전환 등 — 현재는 "내 PC" 고정 + 즐겨찾기 목록으로 충분해 보류
- 전체화면 게임 실행 중 자동 숨김 예외 처리 (필요 시)
- 실제 셸 컨텍스트 메뉴(`IContextMenu`) 완전 연동 — 난이도 높음, 필요성 낮아 보류 (단일 인스턴스 앱 재사용 등 일부 케이스에서 필요할 수 있음)
- 전역 단축키 (포커스 없이 사이드바 호출/토글) — 요청 시 `RegisterHotKey` 기반으로 추가 가능
- **가상 데스크톱(Ctrl+Win+D) 전역 표시**: 새 가상 데스크톱에서도 항상 보이게 — 마이크로소프트 비공식 COM 인터페이스(`IVirtualDesktopPinnedApps`)가 필요하고 윈도우 업데이트마다 깨지기 쉬워 권장하지 않음. 보류
- **파일 없는 폴더 전용 체인 병합 표시**(vs-code compact folders): 기술적으로는 가능하나(체인의 마지막 폴더에 병합 표시명 부여) 즐겨찾기 이동(`FindItemForPath`/`NavigateToPath`) 경로 매칭 로직이 세그먼트 단위 탐색이라 함께 손봐야 함 — 최근 안정화된 즐겨찾기 이동 기능의 회귀 위험 때문에 검토만 완료하고 구현은 보류

## 5. 기술 스택 (권장)

- **플랫폼**: Windows 전용 데스크톱 앱
- **후보 1 (권장): C# / WPF (.NET)**
  - 이유: 창 배치/애니메이션이 표준 기능으로 내장, P/Invoke로 Win32 API(작업영역 조회, ShellExecute, SHGetFileInfo) 연동이 직관적, TreeView 컨트롤 커스터마이징 용이
- **후보 2: Electron + Vue**
  - 이유: 기존 Vue 개발 경험 재사용 가능
  - 단점: 셸 아이콘 연동 등에 네이티브 모듈 필요, 상주 앱치고 메모리 사용량 큼
  - 이번 설계(좌측 고정, 전역 마우스 훅 불필요)에서는 부담이 크게 줄었으므로 채택 가능

## 6. 핵심 구현 포인트 (구현자 참고용)

1. **작업영역(작업표시줄 제외 영역) 조회**
   - WPF: `SystemParameters.WorkArea`
   - 또는 `SHAppBarMessage` (멀티 모니터 대응 시 필요)
2. **창 스타일**
   - `WS_EX_TOOLWINDOW` (Alt+Tab 제외)
   - 테두리/타이틀바 제거 (`WindowStyle=None` in WPF)
3. **폭 토글 애니메이션 및 리사이즈**
   - WPF `DoubleAnimation`으로 Width 속성 트랜지션 (200ms 내외 권장)
   - 우측 경계에 리사이즈 그립(투명 Thumb 등) 배치, 드래그로 Width 직접 조정
   - 마지막 폭 값은 로컬 설정 파일(예: `%AppData%` 하위 JSON)에 저장, 재시작 시 복원
4. **트리 루트 구성**
   - `System.IO.DriveInfo.GetDrives()`로 연결된 드라이브를 최상위 노드로 나열 ("내 PC" 방식)
   - `DriveInfo.IsReady == false`인 드라이브(미디어 없는 광학 드라이브 등)는 회색 처리 또는 목록에서 제외
5. **파일 시스템 트리**
   - 최초 로드 시 전체 스캔 금지, 폴더 펼칠 때마다 하위 1단계만 조회 (지연 로딩)
   - 대용량 폴더(수천 개 파일) 대비 가상화(Virtualizing) 적용 권장
   - `UnauthorizedAccessException` 등 접근 오류는 캐치하여 해당 노드만 스킵 (앱 전체 크래시 방지)
6. **파일 열기**
   - `ShellExecuteEx` + `SEE_MASK_INVOKEIDLIST` 플래그 사용 (탐색기의 더블클릭과 동일한 `IContextMenu` 협상 경로를 태워, 단일 인스턴스 앱이 이미 열린 창을 재사용하는 케이스에서도 더 안정적으로 동작). 연결 프로그램이 없으면 `OpenAs_RunDLL`로 선택 창 폴백
   - 기본 연결 프로그램 그대로 존중, 별도 프로그램 강제 지정 없음
7. **아이콘 (VS Code Material Icon Theme 기반)**
   - 소스: `PKief/vscode-material-icon-theme` (MIT License) — 앱 내 라이선스 고지 유지
   - `material-icons.json` 매핑 파일을 파싱해 확장자/파일명/폴더명 → 아이콘 이름 규칙 그대로 재사용
   - 빌드 타임에 필요한 SVG 아이콘만 선별해 16~20px 기준 1x/2x PNG로 사전 래스터화(예: `resvg`/`Inkscape` CLI 배치 변환) 후 리소스로 임베드
   - 런타임에는 SVG 렌더링 라이브러리 없이 단순 `Image` 컨트롤로 표시 (필요 시 2차에서 SharpVectors 등으로 벡터 렌더링 전환 검토)

## 7. 비범위 (이번 개발에서 제외)

- 전역 마우스 엣지 훅 (좌측 고정 전용이므로 불필요)
- 다중 방향 고정 (상/하/우)
- 클라우드 동기화, 원격 파일 시스템 지원
- 파일 미리보기 (이미지/텍스트 뷰어)

## 8. 개발 우선순위 요약

1. ~~좌측 고정 창 + 폭 토글 애니메이션 + 드래그 리사이즈 (뼈대)~~ — 완료
2. ~~기본 트리뷰(드라이브 루트) + 지연 로딩~~ — 완료
3. ~~더블클릭 열기~~ — 완료
4. ~~Material Icon Theme 기반 아이콘 적용~~ — 완료
5. ~~자유 이동/핀 고정, 모두 접기, 종료 버튼, 폰트 확대축소~~ — 완료 (MVP 범위 밖 추가 구현)
6. ~~즐겨찾기 패널 + 우클릭 컨텍스트 메뉴 확장(열기/연결프로그램/복사/붙여넣기/이름변경/삭제/경로복사/터미널/속성) + 단축키~~ — 완료
7. ~~인라인 이름 바꾸기, 트레이 최소화 + 옵션 메뉴(Auto Collapse/항상 위/시작 프로그램/트레이 아이콘), 헤더 더블클릭 고정 전환, 즐겨찾기 패널 UX 개선~~ — 완료
8. ~~새 폴더/새로고침/정렬 메뉴, 파일 드래그 아웃, 색상 설정 창, 한국어/영어 언어 전환, 정보(About) 창, 시각적 버그 다수 수정~~ — 완료
9. ~~즐겨찾기 단일 클릭 이동 신뢰성 개선: 폴더 표시 25개 제한(+"더 보기")으로 가상화 부담 제거, 대상 트리 맨 위 정렬, 자기유발 선택 변경이 진행 중 walk를 중단시키던 문제(`_navigationToken`) 수정~~ — 완료
10. ~~컨텍스트 메뉴 열린 상태에서 단축키 동작, 느린 더블클릭 이름 바꾸기(파일), "탐색기에서 위치 열기"가 폴더 자신을 열도록 수정(`/select`는 상위 폴더를 여는 동작이라 파일에만 사용)~~ — 완료
11. ~~컨텍스트 메뉴 줄 간격 축소, 옵션 메뉴 재구성(정보를 맨 아래로, 색상 변경/기본 정렬/폴더의 표시 개수/언어 그룹화, 언어에 재시작 안내), 전체 새로고침(F5, 선택 없을 때), 즐겨찾기 폴더 삭제 시 즐겨찾기에서도 제거, 정렬 변경 시 로드된 모든 폴더 재정렬, 폴더 표시 개수 옵션화(1~50, ±버튼), 설정 내보내기/가져오기(즐겨찾기 포함, 없는 경로는 자동 제거), 즐겨찾기 패널 fit의 폰트 크기 반응형, README에 개발 과정 섹션~~ — 완료
12. ~~즐겨찾기 이동의 마지막 원인 규명: 창모드에서 창을 줄이면 다른 드라이브 즐겨찾기가 먹통이 되던 문제 — 드라이브 루트가 루트 패널의 가상화 캐시를 벗어나 컨테이너가 생성되지 않던 것. `CacheLength` + `ScrollUnit="Pixel"`로 해결(위 2절 참고)~~ — 완료
13. ~~옵션 메뉴에 "폴더 아이콘 표시" 토글 추가(폴더 아이콘만 대상, 파일 아이콘은 유지 — 트리와 즐겨찾기 패널 모두 적용, 아이콘 숨김 시에도 탐색기의 글자 시작 위치와 정렬되도록 즐겨찾기 행에 고정 인덴트 칸 분리), "즐겨찾기를 아래에 표시" 토글 추가(고정/창모드 모두 적용 — `Grid.Row`를 옵션값에 따라 동적 재배정, 즐겨찾기 크기/접기/맞춤 로직은 `FavoritesRowDef`로 일반화)~~ — 완료
14. ~~자잘한 개선 묶음: 컬러 팔레트 커스텀 색상이 항목 이동 시 사라지던 버그 수정(`ColorDialog`를 매번 새로 만들어 `CustomColors`가 초기화되던 것 — 정적 필드로 보관), 트리·즐겨찾기 행 hover 시 손가락 커서, 정보 창에 무보증 안내 문구, 색상 설정에 "제목 표시줄 배경" 10번째 항목 추가, 닫기(X) 버튼 우측 여백 확대, 고정 상태 헤더에 앱 아이콘 상시 표시(접힌 44px 레일에서는 화살표 대신 아이콘 자체가 펼치기 버튼 역할 — 화살표+아이콘이 같이 있으면 자리가 부족했음), 헤더 아이콘/텍스트 기본 불투명도를 낮추고 hover 시 밝아지도록(개별 아이콘 색상이 아니라 `ContentPresenter.Opacity` 트리거로 일괄 처리), 배경 3색(탐색영역/즐겨찾기/제목표시줄)을 RGB 26/28/30 단계로, 제목표시줄 하단 구분선 추가(기존 "탭 구분선" 커스텀 컬러 재사용), 탐색기 상단 여백 축소, 앱 아이콘 이미지를 새 `icon.png`로 교체하며 16→20px로 확대, 핀 아이콘 이미지 교체, 헤더 구분선을 2px 더 내리고 아이콘 제외 헤더 요소는 1px 올림(공유 `TranslateTransform` 리소스로 일괄 처리 — 헤더 행 높이·즐겨찾기/탐색기 사이 스플리터는 불변), 즐겨찾기 목록 상단 패딩을 8→6으로 낮춰 탐색기 쪽과 여백 정렬(자동 맞춤 높이 계산 상수도 동기화)~~ — 완료
15. (다음 후보) 콘텍스트 메뉴 이미지 썸네일, 시스템 폰트 선택, 선택/hover 박스 전체 폭 통일(옵션 토글) — 구조상 리스크가 있어 보류, 테마(시스템 다크/라이트 자동 연동), 정보 창에 커밋 해시 표시(빌드 파이프라인 필요), 드래그 앤 드롭으로 파일 이동(트리 내부), 전역 단축키, 폴더 아이콘 숨김 시 파일 아이콘까지 좌측 정렬 맞추는 방식 재검토, 가상 데스크톱 전역 표시, 파일 없는 폴더 체인 병합 표시(vs-code compact folders — 즐겨찾기 이동 로직과 맞물려 회귀 위험, 검토 완료 후 보류)
16. ~~오토하이드(접힘 상태에서 한 번 더 클릭 시 화면 가장자리 슬리버로 숨김, 마우스오버 펼침/핀 고정) + 우측 고정 옵션(기본 정렬/간격 유지, 리사이즈 시 좌측 확장) 추가, 앱 종료/재시작 시 트리 펼침 상태·마지막 선택 위치 복원, 고정 상태 ↔ 창모드 전환 시 직전 위치/크기 기억(세션 내), 앱 아이콘 교체(icon.png 기반, 실행파일/창/트레이/정보창 전부), 색상 기본값 일부 조정, 헤더 버튼(최소화/옵션/닫기) 정렬 수정, 즐겨찾기 fit 하단 여백 축소 + 폰트 크기 변경 시 자동 재적용, 윈도우 시작 시 자동 실행 등록을 매 실행마다 재동기화(exe 경로 변경 후에도 자동 복구)~~ — 완료
17. ~~파일 아이콘 표시 토글(폴더 아이콘 토글과 독립) 추가, 멀티모니터 포커스 고정 구현(주 모니터 고정이던 위치 계산을 창이 실제로 있던 모니터 기준으로 수정, `Screen.FromHandle` + 창 자체 DPI 배율로 환산), 보조 모니터에서 오토하이드가 즉시 닫히던 문제 방어적 완화(재확인 시 OS 커서 좌표를 한 번 더 검증), 색상 설정을 폴더명/파일명 각각 기본·하이라이트·hover 색상(6개)으로 세분화하고 영역 구분선(제목표시줄/즐겨찾기/탐색영역 사이) 색상 신설(총 14개), 색상 설정 창 순서 재정리, 옵션 메뉴 "색상 변경"→"색상 설정" 명칭 통일, "도킹"/"고정" 혼용 표현을 "고정"으로 전면 통일(핀 버튼 툴팁도 좌/우 고정 방향에 따라 동적 표시)~~ — 완료
